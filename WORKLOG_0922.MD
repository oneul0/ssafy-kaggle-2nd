# 작업 로그 — 2026-09-22

> `STRATEFY.MD`의 세부 실행 기록. 어제(9/21)까지 진행 상황은 `STRATEFY.MD` 진행 현황표 참고.

## 0. 어제까지 완료된 것 (요약)

- Qwen3.5-4B, Qwen3-VL-4B 전체 train(5,714장) LoRA 학습 완료 (rank16, bf16, 정답 토큰만 loss, 보기 섞기, 1epoch)
- 두 모델 홀드아웃 평가 + test 6,714장 추론까지 완료, 제출 파일 생성
- 학습 전후 홀드아웃: Qwen3.5 93.3%→94.7%, Qwen3-VL 92.1%→94.0%

## 1. 리더보드 제출 기록 (오늘, 총 7회)

| # | 파일 | 설정 | 홀드아웃 | Public | 판정 |
|---|---|---|---|---|---|
| 1 | q25vl3b_t768.csv (9/21) | Qwen2.5-VL-3B zero-shot | 88.50% | 0.88650 | 기준선 |
| 2 | q35_4b_lora_t768.csv | Qwen3.5-4B LoRA(1단계) | 94.70% | 0.95144 | |
| 3 | q3vl_4b_lora_t768.csv | Qwen3-VL-4B LoRA(1단계) | 94.00% | 0.93893 | |
| 4 | ens_q35lora_q3vllora.csv | 두 LoRA 균등평균 | 95.10% | 0.95680 | 단일 최고 대비 +0.54%p |
| 5 | q25vl7b_4bit_t768.csv | Qwen2.5-VL-7B(4bit) zero-shot | 93.50% | 0.92344 | 앙상블 재료 전용 |
| 6 | **ens_lora2_q7b.csv** | LoRA 2개(1단계) + 7B 균등평균 | 95.60% | **0.95948** | **★ 최종 채택, 지금까지 최고** |
| 7a | q35_4b_lora2_t768.csv | Qwen3.5-4B 2단계 학습 단독 | 95.30% | 0.94697 | 1단계 대비 -0.45%p **악화** |
| 7b | ens_lora2stage_q7b.csv | 2단계q35+1단계VL+7B | 96.00% | 0.95650 | 6번 대비 -0.30%p **악화** |

**핵심 확인 1**: 정상 제출(1~6번)은 홀드아웃-Public 차이가 항상 ±0.6%p 이내. 홀드아웃이 리더보드를 신뢰성 있게 예측함.

**핵심 확인 2 (반례, 7번)**: 2단계 학습은 홀드아웃에서 개선처럼 보였으나(짝비교 p=0.146, 애초에 유의하지 않았음) 실제 리더보드에서는 단독·앙상블 모두 악화됐다. → §5.2, §6 참고.

**현재 최종 채택**: `ens_lora2_q7b.csv` (Public **0.95948**). 오늘 제출 7회 사용(권장 여유 5회 초과) — 남은 제출은 신중하게.

## 2. 보기 회전 TTA 실험

`src/score.py --rot {1,2,3}` 추가(보기를 k칸 순환, 저장 시 원래 순서로 복원). 홀드아웃 1,000장 측정.

| 모델 | rot0 | rot1 | rot2 | rot3 | TTA4 평균 | vs rot0 |
|---|---|---|---|---|---|---|
| Qwen3.5-LoRA | 94.7% | 95.1% | 95.3% | 94.4% | 94.9% | p=0.75 (유의하지 않음) |
| Qwen3-VL-LoRA | 94.0% | 94.5% | 94.9% | 93.7% | 94.6% | p=0.24 (유의하지 않음) |

- 개별 모델 단위로는 효과 불확실. 회전 간 예측 일치율 96~98%로 이미 위치 버릇이 작음.
- 앙상블에 넣으면 LoRA2(TTA없음) 95.1% → LoRA2(TTA4) 95.8%(p=0.12), +7B 96.2%. 방향은 긍정적이나 test 전체 적용 비용(약 4.8시간)이 커서 **보류**. (§6 교훈에 따라 p=0.12는 리더보드 확인 전 신뢰 금지)

## 3. 선지 중의성 스캔 (`src/ambiguity_scan.py`)

Kaggle 디스커션(#742293)의 "한 선지가 다른 선지의 부분문자열" 패턴을 우리 데이터에 재현.

| split | 부분문자열 의심 | 완전동일쌍 |
|---|---|---|
| train (6,714) | 5.02% | 16건 |
| test (6,714) | 5.05% | 19건 |
| holdout (1,000) | 4.40% | 3건 |

| 모델 | 의심후보 정확도 | 나머지 정확도 |
|---|---|---|
| Qwen3.5-LoRA | 93.2% | 94.8% |
| Qwen3-VL-LoRA | **86.4%** | 94.4% |

기존 "4모델 전부 오답 3%"(라벨 노이즈 추정)와 같은 방향의 독립 증거. 정확도 상한(98~99%) 추정을 뒷받침.

## 4. Kaggle 디스커션 조사 (8개 글)

- **규칙(운영진 답변)**: 외부 GPU 대여(Colab 포함) + 공개 가중치 모델 + 자체 코드 학습·추론은 허용. 금지는 외부 API 호출뿐 → Colab 이전 규칙 문제 없음.
- 다른 참가자(A183_정현수)도 독립적으로 로짓 채점·정답토큰 loss·해상도 우선·zero-shot 선측정에 도달 → 우리 방식 교차검증.
- 모델이 클수록 무조건 유리하지 않다, 향상폭 대비 추론비용을 같이 볼 것(A052_하연주) → Colab 30B 학습 전 zero-shot 게이트와 일치.
- 학습량 늘릴 때 정확도만 보지 말고 "고쳐진 문항 vs 망가진 문항" 비율도 볼 것 — 나빠지면 라벨 노이즈 학습 신호.
- Colab(대형 모델 Main) + 로컬(specialist 앙상블) 구조(서울_22반_조용필) — 우리 계획과 동일.

## 5. 로컬 레시피 확정 실험

### 5.1 LoRA rank 16 vs 64 (부분학습 1,500장, Qwen3-VL-4B, alpha 2배 유지)

| rank | 홀드아웃 |
|---|---|
| 16 (alpha 32) | **94.4%** |
| 64 (alpha 128) | 93.1% |

rank16만 맞음 21 : rank64만 맞음 8, **p=0.024 유의**. 소규모 데이터에서 rank64는 과적합. → **rank 16 확정** (Colab 큰 모델에도 그대로 적용).

### 5.2 2단계 학습 — 최종 기각 (반례로 확정)

`train_lora.py`에 `--init-adapter`(완성 어댑터를 초기값으로 로드) 추가, lr 2e-5·0.3epoch로 이어서 학습.

| 모델 | 1단계 | 2단계(홀드아웃) | 짝비교 | **리더보드 실측** |
|---|---|---|---|---|
| Qwen3.5-4B | 94.7% | 95.3% | p=0.146 | **0.94697 (-0.45%p 악화)** |
| Qwen3-VL-4B | 94.0% | 93.4% | p=0.210(악화) | (제출 안 함, 홀드아웃부터 악화) |

**결론: 2단계 학습은 최종 레시피에서 제외.**

### 5.3 최종 확정 레시피 (Colab에 그대로 이식)

| 항목 | 값 |
|---|---|
| 해상도 | 768~880토큰 (원본 크기) |
| 채점 방식 | 생성 대신 a/b/c/d 로짓 비교 |
| LoRA rank/alpha | **16 / 32** |
| loss | 정답 토큰 1개에만 (프롬프트·이미지는 -100) |
| 보기 순서 | 매 샘플 무작위 셔플 + 정답 위치 동기화 |
| epoch | **1epoch만** (2단계 저lr 재학습 금지) |
| lr | 1e-4, cosine, warmup 3% |
| 학습 대상 | 언어모델 부분만 (비전 타워 동결) |
| 압축 | 가능하면 미압축(bf16). 불가피하면 압축 손실(−1.9~−3.2%p) 감수 |

## 6. 이번 세션의 핵심 교훈

홀드아웃 1,000장 짝 비교에서 **p≥0.1인 개선은 리더보드에서 뒤집힐 수 있다** (2단계 학습 사례로 실측 확인). 앞으로 개선 후보는:
1. 리더보드로 실측 확인하거나,
2. 최소 2개 이상의 독립 실행(다른 seed)에서 같은 방향으로 재현될 때만

최종 레시피에 반영한다 (`STRATEFY.MD` §6.3 규칙을 이번에 어겨서 직접 확인한 대가).

## 7. Colab 세팅 추천

### 7.1 목표 구성

| 항목 | 추천 | 근거 |
|---|---|---|
| GPU | **A100 40GB (Colab Pro/Pro+)** | 이미 보유 중인 등급 |
| 모델 | **Qwen3-VL-30B-A3B-Instruct** (MoE, 총 30B/활성 3B) | 4bit면 가중치 15~18GB로 40GB에 여유. MoE라 토큰당 계산은 3B급이라 dense 27~32B보다 학습이 훨씬 빠름. `qwen3_vl_moe` 아키텍처는 로컬 `baseline`(transformers 4.57.6)/`baseline5`(5.17.0) 양쪽에서 이미 지원 확인됨 |
| 대안(백업) | Qwen2.5-VL-32B-Instruct(dense) | zero-shot 게이트에서 MoE가 기대 이하일 때만 고려. 계산량이 커서 학습이 느림 |
| HF repo id | **`Qwen/Qwen3-VL-30B-A3B-Instruct`** ✅ 확인 완료 | 로딩 클래스 `Qwen3VLMoeForConditionalGeneration`, 약 31B/BF16, 다운로드 약 60GB |
| 라이선스 | **Apache 2.0** ✅ 확인 완료, 상업·교육 목적 사용 허용 | 기존 사용 중인 Qwen 모델들과 동일 |

### 7.2 환경 준비 체크리스트

1. **버전 고정**: 로컬 `baseline5`에서 이미 검증된 조합을 그대로 사용 — `transformers 5.17.0`, `peft 0.21.0`, `accelerate 1.15.0`, `bitsandbytes 0.50.2` (`outputs/baseline5_requirements.txt` 참고). Colab은 인터넷이 있으니 `pip install` 그대로 가능 (로컬처럼 오프라인 wheel 준비 불필요).
2. **코드 이식**: `src/common.py`, `src/score.py`(로짓 채점 + `--rot`), `src/train_lora.py`(정답토큰 loss + `--init-adapter`), `src/ambiguity_scan.py`, `src/ensemble.py`류를 Drive 또는 저장소 clone으로 옮긴다. **제공된 `(260908)_baseline_colab.ipynb`는 생성 방식 베이스라인**이라 그대로 쓰지 말고 우리 로짓 채점 스크립트로 교체.
3. **⚠️ 코드 수정 필요 항목**: `train_lora.py`는 현재 bf16 미압축 로드만 지원한다(`score.py`처럼 `--quant` 옵션이 없음). 30B를 40GB에 학습하려면 **QLoRA 경로 추가**가 필요함 — `BitsAndBytesConfig(4bit)` + `prepare_model_for_kbit_training` 추가. Colab 작업 시작 전에 이 부분부터 구현.
4. **데이터**: `data/`(train/test 이미지+csv), `outputs/split/`(`holdout.csv`, `train_fit.csv`)를 **로컬과 동일 파일**로 Drive에 올린다. 홀드아웃이 다르면 로컬 결과와 비교가 깨짐.
5. **모델 다운로드**: 약 60GB, Colab 세션마다 새로 받지 않도록 Google Drive에 캐시(또는 `/content` 로컬 디스크에 받고 세션 끊기면 재다운로드 감수).

### 7.3 실행 순서 (Gate 방식, §STRATEFY.MD Phase 1~2와 동일)

1. **zero-shot 게이트**: 학습 없이 4bit로 홀드아웃 1,000장 채점 (`score.py --quant 4bit`, 기존 옵션 그대로 사용 가능). **로컬 최종 앙상블(95.6%)/최고 단일(94.7%)과 비교**해서 압축 손실을 감안해도 확실히 나은지 먼저 확인. 못 넘으면 학습에 시간 쓰지 말고 앙상블 재료로만 사용.
2. **QLoRA 학습**: 게이트 통과 시 §5.3 레시피 그대로(**rank16/alpha32, 정답토큰 loss, 보기셔플, 1epoch, 2단계 학습 없음**) 적용. A100은 메모리 여유가 있으니 batch를 늘려 속도를 올려도 됨(단, 한 번에 크게 올리지 말고 조금씩 — Colab OOM은 로컬보다 흔하다는 디스커션 경험담 참고).
3. **세션 제한 대응**: Colab 12시간 제한/유휴 종료 대비, `--resume`을 반드시 사용하고 `--save-every`를 짧게, 체크포인트를 Drive에 저장.
4. **채점·통합**: 같은 `holdout.csv`로 채점한 확률 파일을 로컬로 가져와 기존 `ensemble.py`/`make_ensemble_sub.py`로 통합. **개선처럼 보여도 §6 교훈에 따라 리더보드로 실측 확인 전엔 확정하지 않는다.**

## 8. 다음 할 일

- [x] `train_lora.py`에 QLoRA(4bit) 경로 추가 — `prepare_model_for_kbit_training` 적용, Qwen3-VL-4B 4bit 스모크 테스트로 lora_B 가중치 실제 갱신 확인 완료 (조용한 미학습 방지, Kaggle #742458 교훈 반영)
- [x] Qwen3-VL-30B-A3B 정확한 repo id/라이선스 확인 완료 (`Qwen/Qwen3-VL-30B-A3B-Instruct`, Apache 2.0)
- [ ] Colab에서 zero-shot 게이트 실행 (4bit, 홀드아웃 1,000장, 로컬 최고 95.6% 대비 비교)
- [ ] Phase 4 재현성 검증(깨끗한 환경에서 동일 결과) — 로컬·Colab 둘 다 아직 미착수
- [ ] 남은 제출(오늘 7회 이미 사용) 배분 계획

## 9. 이번 세션에서 추가/수정한 코드

- `src/score.py`: `--rot k` (TTA용 보기 회전) 옵션 추가
- `src/train_lora.py`: `--init-adapter` (2단계 학습용 기존 어댑터 이어받기), `--quant {4bit,8bit}` (QLoRA, Colab 30B급 대비) 옵션 추가
- `src/ensemble.py`, `src/make_ensemble_sub.py`: 홀드아웃 앙상블 비교, test 제출 파일 생성
- `src/tta_eval.py`: TTA 결과 분석
- `src/ambiguity_scan.py`: 선지 중의성(부분문자열) 스캔
- `src/run_test_7b.sh`, `src/run_tta_holdout.sh`, `src/run_recipe_confirm.sh`, `src/run_test_q35_2stage.sh`: 백그라운드 파이프라인 스크립트
- `outputs/leaderboard_log.csv`: 모든 제출 Public 점수 기록
