# 🎯 VQA 정확도 극대화 전략 (STRATEGY)

> **목표**: 사진 속 글자·물체에 대한 4지선다(a~d) 문제를 **가장 많이 맞히는 것** (평가 지표: 정확도)
> **작성 근거**: ① 프로젝트 폴더 분석 ② 내 PC(RTX 5060 Ti 16GB)에서 직접 측정 ③ 비슷한 대회 선행 풀이 7건 분석 ([REFERENCE_SOLUTIONS.MD](REFERENCE_SOLUTIONS.MD)) ④ 5개 모델 비교 실험 ([MODEL_COMPARISON.MD](MODEL_COMPARISON.MD))
> **코드**: `src/` ([사용법](src/README.MD)) · **최신 진행 상황**: 아래 "0. 한눈에 보기 → 진행 현황"

## 📖 이 문서 읽는 법

- 💬 **쉬운 설명** 박스: AI를 잘 몰라도 이해할 수 있게 풀어 쓴 설명입니다. 어려우면 이것만 읽어도 흐름을 잡을 수 있습니다.
- 처음 보는 용어는 바로 아래 **[용어 사전](#-용어-사전-먼저-읽어-보세요)** 에 있습니다.
- 근거의 종류를 표시했습니다.

| 표시 | 뜻 | 믿을 정도 |
|------|-----|-----------|
| **[실측]** | 이 폴더의 데이터·모델로 **직접 측정**한 값 | 높음 (단, 표본이 작으면 오차 있음) |
| **[선행사례]** | 비슷한 대회 참가자들이 **직접 밝힌 경험** | 중간 (자기 보고, 도메인이 다를 수 있음) |
| **[가설]** | 일반적인 경험으로 **예상**한 것. 이 데이터에선 아직 검증 전 | 낮음 → 실험으로 확인 필요 |

---

## 📚 용어 사전 (먼저 읽어 보세요)

| 용어 | 쉬운 설명 |
|------|-----------|
| **VQA / VLM** | 사진을 보고 질문에 답하는 AI. **VLM**은 "눈(사진 이해)과 입(글 이해)을 함께 가진 AI 모델"을 부르는 이름입니다. (예: Qwen-VL) |
| **사전학습 모델** | 이미 방대한 데이터로 공부를 끝낸 AI. 우리는 이걸 **가져다 쓰기만** 해도 시작점이 꽤 높습니다. |
| **파인튜닝 (Fine-tuning)** | 똑똑한 학생(사전학습 모델)에게 **이 대회 기출문제(train)로 추가 과외**를 시키는 것. |
| **LoRA** | 모델 전체를 다시 가르치면 너무 무겁고 오래 걸립니다. 그래서 모델에 **작은 "메모장"만 덧붙여서 그 메모장만 학습**시키는 기법. 싸고 빠릅니다. |
| **양자화 (4bit, 8bit)** | 모델을 **압축**해서 작은 GPU 메모리에 올리는 기술. 용량은 크게 줄고 성능은 아주 조금 떨어집니다. (사진을 JPG로 압축하는 것과 비슷) |
| **QLoRA** | "양자화(압축)한 모델 + LoRA(메모장)" 조합. 16GB 같은 작은 GPU에서 큰 모델을 학습할 때 씁니다. |
| **GPU 메모리(VRAM)** | AI를 돌리는 그래픽카드의 작업 공간. 우리는 **16GB**. 모델이 이보다 크면 못 올립니다. |
| **해상도 / 토큰(조각)** | 사진의 선명도. 간판의 **작은 글씨는 화질이 낮으면 아예 못 읽습니다.** 이 모델은 사진을 **28×28 픽셀 "조각(토큰)"** 으로 잘라서 보며, **조각 수 = 사진 크기**입니다. 우리 사진(720×960)의 원본 크기는 약 **880 토큰**이고, 이보다 작게 설정하면 사진을 **줄여서** 보는 것, 크게 설정하면 **늘려서** 보는 것입니다 (§4.2 표 참고). |
| **로짓 스코어링** | AI에게 말로 답하라고 시키지 않고, **a·b·c·d 각각에 "얼마나 확신하는지" 점수를 매기게** 해서 가장 높은 걸 고르는 방식. 말이 이상하게 나올 일이 없고, 점수(확률)도 남습니다. |
| **위치 편향** | AI가 정답 내용과 상관없이 **특정 자리(예: d)를 덜 고르는 버릇.** |
| **TTA (선지 회전)** | **같은 문제를 보기 순서만 바꿔서 여러 번 풀게 하고 평균**을 내는 것. 사람이 "보기를 섞어서 다시 풀어보는 것"과 같습니다. 순서 버릇을 상쇄합니다. |
| **앙상블** | 여러 AI의 답(점수)을 **평균 내서 결정**하는 것. 각자 실수하는 문제가 달라서 합치면 더 정확해집니다. |
| **홀드아웃(모의고사)** | 학습에 **일부러 쓰지 않고 빼놓은 문제**. 기출문제로 공부한 뒤 이 모의고사로 진짜 실력을 잽니다. |
| **과적합** | 기출문제를 **달달 외워서** 연습문제 점수는 높은데 새 문제는 못 푸는 상태. |
| **Public / Private 리더보드** | 시험 점수판이 둘입니다. **Public**은 대회 중 보이는 점수(테스트의 일부), **Private**은 대회 끝나고 공개되는 **진짜 최종 점수**(나머지 부분). Public에만 맞추면 최종에서 순위가 흔들립니다. |
| **OCR** | 사진 속 글자를 읽어내는 기술. 이 대회 문제는 대부분 이 능력이 핵심입니다. |
| **의사라벨(pseudo-label)** | AI가 **추측한 답을 정답인 척** 학습에 쓰는 것. 추측이 틀리면 오히려 해롭습니다. |
| **오프라인 환경** | 대회 당일 **인터넷이 안 되거나 막힐 수 있어서**, 필요한 모델·프로그램을 미리 다 받아두어야 한다는 뜻. |

---

## 0. 한눈에 보기

> 💬 **쉬운 설명**
> 이 대회는 "생각을 잘하는" AI가 아니라 **"사진 속 글자를 정확히 읽는" AI**를 뽑는 시험입니다. (예: 간판 글씨, 가격표 숫자) 게다가 오답 보기가 `NAIL SALON` vs `NAIL SHOR`처럼 **한두 글자만 다르게** 만들어져 있어서, **사진을 선명하게 보여주는 것**이 제일 중요합니다.
> 이미 제공된 AI(Qwen2.5-VL-3B)는 **아무것도 안 가르쳐도 90%**를 맞혔습니다(직접 측정). 그래서 앞으로의 싸움은 "90%를 95%로 올리는 것"입니다. 비슷한 대회 1등도 **큰 모델 + 짧은 추가 학습 + 보기 섞어 풀기** 조합이었습니다.

### 진행 현황

| 단계 | 상태 | 핵심 결과 |
|------|:---:|-----------|
| Phase 0 기반 구축 | ✅ 완료 | 홀드아웃 1,000장 분리, 채점기(`src/score.py`), 기준선(3B) **88.5%**, test 제출 → **Public 0.88650** (홀드아웃과 +0.15%p 차이, **홀드아웃이 리더보드를 잘 예측함**) |
| Phase 1 모델 탐색 | ✅ 완료 | 신형 4종 **92.1~93.5%** (기준선 대비 확실히 우수, 서로는 구분 불가), 4개 평균 **94.9%**. 압축(4bit)은 정확도 **−1.9~−3.2%p** → 4B는 압축 없이 학습 |
| Phase 2 LoRA 학습 | 🔄 진행 중 | Qwen3.5-4B(주력) → Qwen3-VL-4B(보조) 순서로 전체 train LoRA(bf16). 각 약 2~2.5시간 |
| 선지 회전 TTA | ⏳ 대기 | 학습 후 구현·측정 |
| Phase 3·4 앙상블·마무리 | ⏳ 대기 | |

**지금까지의 핵심 발견 [실측]**
1. 사진을 원본 크기(≈768~880 토큰)로 맞추면 충분하고, 그 이상은 이득이 확인되지 않음 (§4.2)
2. 새 세대 모델은 기준선(3B)보다 확실히 좋고, 특히 3B가 약했던 **부정형 문제(57.7%)** 를 크게 개선 (§2.3)
3. **QLoRA는 정확도를 올리지 않음** — 압축 자체가 정확도를 깎고, 4B는 압축 없이 16GB에서 학습됨 (§4.4)
4. 4개 신형 모델은 오답이 3분의 1만 겹쳐 **앙상블 효과가 큼** (94.9%). 단 학습 후 재확인 필요 (§4.6)
5. GPU 메모리 함정: 실제 사용량은 12GB인데 **예약 메모리가 16GB를 넘으면 10배 이상 느려짐** (§4.4)

### 우선순위 요약

| 순위 | 할 일 | 왜? | 근거 | 비용 |
|:---:|-------|-----|:---:|------|
| 1 | **사진을 원본 크기(768~880 토큰)로 맞추기** | 3B 모델이 80% → 92% (베이스라인은 원본의 절반 이하로 줄여서 봄) | **[실측]** | 낮음 (시간 ↑) |
| 1 | **점수(로짓) 방식 + 위치 버릇 보정** | AI가 d를 덜 고르는 버릇 확인 | **[실측]** + **[선행사례]** | 낮음 |
| 2 | **학습 때 보기를 무작위로 섞기** | 위치 버릇의 근본 해결. 상위권 공통 | **[선행사례]** | 낮음 |
| 2 | **보기 회전 TTA** (올바른 구현) | 1등의 핵심 요소 (TTA4로 0.951) | **[선행사례]** | 중 (추론 2~4배) |
| 2 | **정답 칸만 복습시키기 (정답 토큰 loss)** | 상위권 3명이 공통으로 사용, "가장 효과적"이라 평가 | **[선행사례]** | 낮음 |
| 3 | **전체 train으로 LoRA 학습 (압축 없이 bf16)** | 제공 베이스라인은 200장만 씀. QLoRA는 정확도를 깎으므로 4B는 불필요 | **[가설]** + 선행사례 + **[실측]**(메모리·압축 손실) | 중~높음 (약 2~2.5시간) |
| ✅ | **더 새롭고 좋은 모델 고르기** (완료) | 3B 88.5% → 신형 92.1~93.5%, 부정형 57.7% → 88~94% | **[실측]** | 완료 |
| 4 | **2단계 학습** (약한 세기로 한 번 더) | 1시간 투자로 최종 점수 개선 사례 | **[선행사례]** | 낮음 |
| 4 | **여러 AI 평균 (앙상블)** | 강한 모델끼리는 이득 | **[선행사례]** | 높음 (시간) |
| 5 | OCR 붙이기, dev 의사라벨, 복잡한 선택 규칙 | 선행 사례에서 대부분 실패·미미 | **[선행사례]** | 높음 |
| ✖ | **CoT(길게 생각해서 풀기)** | 대부분 "읽기" 문제라 이득 작고 오래 걸림 | **[가설]** | 매우 높음 |

**주의**: 해상도는 **"원본 크기까지 되돌리는 것"** 이 핵심이고, **원본보다 더 키우는 것은 효과가 확인되지 않았습니다** (768 → 1024 차이 없음, 768 → 1536은 +4문제로 우연 범위, 시간은 2~3배). 그래서 기본은 **768~880**, 더 높은 해상도는 **"헷갈리는 문제에만"** 선택적으로 씁니다 (§4.2).

---

## 1. 데이터 진단 [실측]

> 💬 **쉬운 설명**
> 문제집(데이터)을 뜯어봤습니다. 정답이 a·b·c·d 골고루라 "찍는 패턴"은 없고, 오답 보기는 정답과 거의 비슷해서 헷갈리게 만들어져 있으며, 대부분 사진 속 글자를 읽는 문제입니다. 계산이나 추론이 필요한 문제는 거의 없습니다.

### 1.1 규모와 구성

| 항목 | 값 |
|------|-----|
| train (학습용, 정답 있음) | 6,714행 |
| test (제출용, 정답 없음) | 6,714행 |
| dev (개발용, `answer1~5`는 교육생 응답이지 정답 아님) | 2,683행 |
| 이미지 | 대부분 폭 720px, **세로형 74%**, 중앙값 720×960 (train 합계 약 0.75GB) |

### 1.2 문제 특성

| 특성 | 수치 | 뜻 |
|------|------|-----|
| 정답 분포 | a 25.3 / b 24.5 / c 25.6 / d 24.6 (%) | **자리로 찍을 수 없음.** 위치 버릇은 AI의 결함 |
| 가장 긴 보기가 정답 | **35.3%** (무작위면 25%) | 약한 길이 힌트가 있음. 너무 믿으면 안 됨 |
| 질문 / 보기 길이 | 평균 32자 / 약 9자 | 보기가 짧은 글자 조각 → **글자 하나하나 정확히 읽어야 함** |
| 보기 언어 | 한글 63%, 숫자 19%, 영문 17% | 한국어 + 영어 + 숫자 (일본어 등 일부) |
| 질문 유형(키워드 근사) | "무엇" 70%, 따옴표 인용 22%, 가격·수량 16%, 부정형("아닌 것") 5%, 색·모양 5%, 위치("가장 오른쪽") 4%, 계산 <1% | **대부분 단순 판독형** |
| 오답 보기 | `NAIL SALON` vs `NAIL SHOR`, `$6.98/5.98/8.98/7.98` | **한두 글자 차이** → 저화질에서 치명적 |
| 중복 | train 내 중복 7장, test 이미지 24장이 train에도 있음 (질문까지 같은 건 1건) | 영향 미미. 모의고사 분할은 **이미지가 같으면 같은 쪽**으로 |

### 1.3 dev 데이터의 함정 ⚠️

> 💬 **쉬운 설명**
> dev에는 "교육생 5명이 푼 답"이 들어 있는데, 이건 **정답이 아니라 학생들이 푼 답**입니다. 그리고 학생들 답이 꽤 틀립니다. 그래서 정답처럼 믿고 쓰면 위험합니다.

- 응답에는 결측이 있음 (5명 응답 2,287행 / 4명 364행 / 3명 이하 32행).
- 5명 중 **같은 답을 낸 최대 인원**: 3명 2,026행 / 2명 649행 / 1명 이하 8행 / **4명 이상 0건**.
- **AI(정답 기준 90%)가 고른 답과 학생 다수결의 일치율은 53~57%뿐** → 다수결의 정확도는 대략 60% 안팎일 가능성 (정확값은 알 수 없음).
- **1회차 대회와의 차이 (중요)**: 1회차 dev에는 "5명 중 4명 이상 일치"가 902건이나 있어서 믿을 만한 라벨로 썼지만, **우리 dev에는 그런 행이 0건**입니다. 같은 방법을 쓸 수 없습니다.
- 결론: **dev를 모의고사나 정답으로 쓰지 마세요.**

---

## 2. 현재 위치 파악

### 2.1 아무 학습 없이 풀어 본 결과 [실측]

> 💬 **쉬운 설명**
> 제공된 AI에게 **추가 공부 없이** 바로 문제를 풀려 봤습니다. 사진을 **작게 줄여서(베이스라인 방식)** 보여주면 82%, **거의 원본 크기로** 보여주면 90%를 맞혔습니다. 또 이 AI는 **d를 잘 안 고르는 버릇**이 있었습니다(정답의 25%가 d인데 15%만 선택).

측정 방식: train에서 150장 무작위 추출, 파인튜닝 없음, 베이스라인과 같은 프롬프트, a/b/c/d 4개 점수 비교.

| 설정 | 사진 크기 | 정확도 | 속도 | test 6,714장 걸리는 시간 |
|------|-----------|:---:|:---:|:---:|
| 베이스라인 방식 | 384×384 (≈188 조각) | **82.0%** | 0.15초/장 | ≈ 17분 |
| 원본 근접 | ≈768 조각 (원본의 약 93%) | **90.0%** | 0.44초/장 | ≈ 49분 |

- 예측한 정답 자리 분포: 384px `a43 b38 c47 d22`, 768 `a40 b40 c46 d24` (150장 중) → **d를 15~16%만 선택 (기대 25%)**.
- ⚠️ **표본이 150장이라 오차가 ±5%p 정도**입니다. "방향(원본 크기로 되돌리면 좋아진다)"은 믿을 만하지만 **수치를 목표로 삼지는 마세요.** 1,000장 모의고사로 다시 재야 합니다.
- **해상도를 6단계로 더 자세히 잰 결과(300장)는 §4.2에 있습니다.** 요약: 원본(≈880 토큰)까지는 오르고, 그 이상은 차이가 없거나 우연 범위입니다.
- **아무 학습 없이 90%** → 남은 10%가 진짜 싸움. 그 10% 중 일부는 **사람도 못 맞히는 문제(사진이 흐리거나 정답이 모호)** 일 수 있으니 **틀린 문제를 직접 눈으로 보는 것**이 먼저입니다 (§4.9).

### 2.2 제공된 베이스라인 코드의 개선 여지

> 💬 **쉬운 설명**
> 베이스라인은 "이렇게 하면 된다"는 **맛보기 예제**입니다. 그래서 200문제만 공부시키고, 사진도 작게 줄이고, 채점 방식도 단순합니다. 아래는 "진짜로 점수를 올리려면 바꿔야 할 곳" 목록입니다.

`(260902)_baseline_desktop5060ti_offline.ipynb` 기준.

| # | 지금 | 문제 / 개선 방향 |
|---|------|------------------|
| 1 | 학습 데이터 **200장**, 1 epoch(한 바퀴), 약 50번 업데이트 | 전체 6,714장을 쓰면 배울 것이 30배 이상 |
| 2 | `labels = input_ids.clone()` | **문제 지문·사진 부분까지 채점 대상**이라 정작 중요한 "정답 한 글자" 학습이 묻힘. **정답 칸만 남기고 나머지는 채점에서 제외(`-100`)** 해야 함 |
| 3 | 사진을 384×384로 고정 | **실측 -8%p.** 원본은 720×960 |
| 4 | 4bit로 압축해서 로드 | 3B 모델은 압축 없이도 16GB에 올라감 → **압축 손실이 불필요**. 7~8B급을 쓸 때만 QLoRA |
| 5 | `GradScaler(fp16)` + `autocast(bfloat16)` 혼용 | 서로 다른 정밀도를 섞어 쓰는 불필요한 조합 |
| 6 | 글로 답을 생성 → 파싱, 실패하면 `"a"`로 처리 | 파싱 실패가 조용히 `a`가 됨. **로짓 스코어링**이면 실패가 없고 확률도 얻음 |
| 7 | 검증은 loss만, 검증 표본 20장 | **정확도**를 재는 모의고사가 필요 |
| 8 | 한 번에 1문제씩 학습 | 여러 문제를 모아(grad accumulation) 유효 배치 8~16 정도로 (**메모리 때문에 한 번에 올리는 문제 수는 1~2가 한계**, §4.4) |

### 2.3 홀드아웃 기준선과 모델 대결 [실측]

> 💬 **쉬운 설명**
> 앞의 82%·90%는 150~300문제로 잰 대략치였습니다. 학습에 쓰지 않은 **1,000문제 모의고사**로 다시 재니 **3B는 88.5%**였습니다. 이어서 새 세대 모델 4개를 같은 시험에 넣어 보니 **92~93.5%로 모두 확실히 더 잘 봤고**, 특히 3B가 거의 못 풀던 **"~가 아닌 것은?" 문제**(57.7점)를 크게 개선했습니다. 4개끼리는 점수 차이가 작아서 **누가 더 낫다고 말할 수 없었습니다.** 대신 서로 **다른 문제를 틀려서**, 4개를 평균 내면 94.9%까지 올랐습니다.

조건: 홀드아웃 1,000장, 학습 없음, 사진 672×896px, a/b/c/d 점수 방식, 프롬프트 동일. 상세는 [MODEL_COMPARISON.MD](MODEL_COMPARISON.MD).

| 모델 | 정확도 | 95% 신뢰구간 | 부정형 | 장당 | 비고 |
|------|:---:|:---:|:---:|:---:|------|
| Qwen2.5-VL-3B (기준선) | 88.5% | 86.4~90.3 | 57.7% | 0.43초 | |
| **Qwen3.5-4B** | **93.3%** | 91.6~94.7 | 88.5% | **0.36초** | 학습 주력 |
| Qwen3-VL-4B | 92.1% | 90.3~93.6 | 94.2% | 0.39초 | 학습 보조 |
| Qwen3-VL-8B (4bit) | 92.9% | 91.1~94.3 | 88.5% | 0.61초 | 추론 전용 |
| Qwen2.5-VL-7B (4bit) | 93.5% | 91.8~94.9 | 90.4% | 0.69초 | 추론 전용 |
| **신형 4개 평균** | **94.9%** | | | 합계 약 2초 | 앙상블 (학습 전) |

- 3B와 신형 4종의 차이는 통계적으로 확실(p≤0.001), **신형끼리는 구분 불가**(p=0.11~0.90).
- 홀드아웃 오답 115건 중 22건이 부정형이었음(3B). 문제 수가 적은 유형(부정형 52·색/모양 45·위치 33)은 경향으로만 볼 것.
- **정답이 d일 때** 3B는 78.6%로 약했고(다른 자리 91.6%), 신형은 87~91%로 격차가 줄었음. 그래도 전 모델이 d를 25%보다 덜 고름(21.8~23.1%).
- **신형 4개가 모두 틀린 문제는 19개(1.9%)**. 직접 본 4장 중 3장은 정답이 모호하거나 라벨 의심 → 실질 상한은 98% 안팎일 수 있음 (전수 확인 필요: `outputs/probs/holdout_all_new_models_wrong.csv`).
- **확신도**: Qwen3.5-4B·Qwen2.5-VL-7B는 오답의 88~94%가 낮은 확신도에 몰려 신뢰 가능, **Qwen3-VL 계열은 틀린 것의 56~61%를 확신 0.9 이상으로 틀려** "헷갈리는 문제만 골라 재추론" 전략에 부적합.

---

## 3. 비슷한 대회에서 배운 것

> 💬 **쉬운 설명**
> 작년에 **거의 똑같은 SSAFY 대회(재활용품 사진 버전)** 가 있었고, 상위권 사람들이 풀이를 GitHub에 공개했습니다. 7개를 읽어 보니 **1등부터 30위권까지 하는 방법이 놀랄 만큼 비슷**했습니다. 이 문서의 상당 부분이 그 공통점을 따릅니다. (자세한 내용: [REFERENCE_SOLUTIONS.MD](REFERENCE_SOLUTIONS.MD))

### 3.1 어떤 대회가 우리와 비슷한가

| 대회 | 우리와 비슷한 정도 | 이유 |
|------|:---:|------|
| **SSAFY 16기 1회차** (재활용품 사진, Kaggle) | ⭐⭐⭐ 거의 같음 | 4지선다, 한국어 질문, Qwen 베이스라인, 하루 20회 제출, dev에 교육생 응답 5개 |
| SCPC 2025 (삼성, DACON) | ⭐ 형식만 같음 | **모델 총 3B 미만 제한 + 학습 데이터 60장** → 가지치기·증류 같은 다른 싸움 |

### 3.2 상위권의 공통 레시피 [선행사례]

1. **Qwen 계열 VLM + LoRA/QLoRA** (전원)
2. **정답 토큰에만 채점** (3명이 사용, 1명은 "가장 효과적"이라 평가)
3. **a/b/c/d 점수 비교(로짓 스코어링)** (전원)
4. **학습 때 보기 순서를 무작위로 섞기** (1등 포함)
5. **추론 때 보기 순서를 돌려가며 여러 번 풀고 평균 (TTA)** (1등 포함)
6. **이미지 해시(지문) 기준으로 모의고사 분리** (누수 방지)

### 3.3 점수를 가른 것 [선행사례]

| 모델 | 점수 | 비고 |
|------|---:|------|
| Qwen3-VL-4B QLoRA | 0.921 (Public) | 내 PC와 같은 GPU에서 약 79분 학습 |
| Qwen3.5-4B QLoRA | 0.917 (Public) / **0.927 (Private)** | 내 PC와 같은 5060 Ti |
| Qwen3-VL-8B | 0.924 / 0.9255 | 서로 다른 두 사람 |
| 35B급 + 8B급 + TTA + 앙상블 | 0.943 (Public) | A100 GPU |
| **27B + TTA4 (1등)** | 0.951 (Public) → **0.957 (Private)** | A100 80GB |

> 💬 **쉬운 설명**
> 4B에서 8B로 키워도 거의 그대로(+0.3%p)인데, 27B급 + 보기 회전을 쓰면 3%p나 올랐습니다. 하지만 **우리 GPU(16GB)로는 27B를 못 씁니다.** 그래서 현실적인 목표는 **0.92~0.94 부근**(도메인이 달라 참고용)이고, 이걸 넘으려면 **작은 모델의 약점(글자 판독)을 해상도와 학습으로 메우는 것**이 관건입니다.
> ⚠️ 서로 다른 사람이 서로 다른 설정으로 한 실험이라 **엄밀한 비교는 아닙니다.**

### 3.4 반복적으로 실패한 것들 [선행사례]

| 시도 | 결과 |
|------|------|
| dev 다수결로 **의사라벨** 학습 | 학습 손실은 좋아졌는데 **점수는 하락** (0.921→0.919) |
| **"어느 모델을 믿을지" 고르는 복잡한 규칙(라우터)** | 검증 점수만 오르고 실제(Public)는 하락 |
| 해상도를 여러 개 섞어 **990~2,448가지 조합을 탐색** | 작은 검증셋에 **과적합**, Public 하락 |
| 질문 문구로 **정답 확률 보정(텍스트 prior)** | 과적합, 하락 |
| **약한 모델과 강한 모델 블렌딩** | 오히려 손해 |
| 보기 회전을 **배치 + 오른쪽 패딩**으로 잘못 구현 | 원본과 회전 예측이 **36%만 일치**, 결과 폐기 |
| 하루 제출 20회를 다 써 버림 | **가장 유망한 후보를 못 냄** |

> 💬 **쉬운 설명**
> "똑똑해 보이는 복잡한 기법"은 대부분 **연습 시험(검증)에서만 잘 나오고 진짜 시험에서는 안 통했습니다.** 단순하게 **큰 모델 + 짧은 학습 + 보기 회전 평균**이 오래 살아남았습니다. 그리고 **Public 점수만 보고 고치다 보면** 최종(Private)에서 순위가 떨어질 수 있습니다.

---

## 4. 기술 선택지 검토

### 4.1 모델 선택

> 💬 **쉬운 설명**
> "어떤 AI를 쓸까?"의 문제입니다. 큰 모델일수록 대체로 똑똑하지만 **우리 그래픽카드(16GB)에 안 들어가면 못 씁니다.** 크기 대략 계산법: **파라미터 수(B) × 2 = GB** (예: 3B → 약 6~7GB, 8B → 약 16GB). 학습은 여기에 작업 공간이 더 필요하고, 압축(4bit)하면 대략 1/4로 줄어듭니다.

| 후보 | 크기 | 16GB 적합성 | 이 과제 기대 | 비고 |
|------|------|-------------|-------------|------|
| **Qwen2.5-VL-3B** (이미 보유) | 3B (7.1GB **[실측]**) | 추론·학습 모두 여유 | 학습 없이 82~90% **[실측]** | 기준선. **Qwen 연구용 라이선스** (§7) |
| **Qwen3-VL 4B** | 4B ≈ 9GB | **압축 없이 학습 가능** (배치 1~4, 메모리 10.9~15.6GB) **[실측]** | **92.1%** (학습 전) **[실측]** | 기존 환경(`baseline`)에서 사용. Apache-2.0. 확신도 과신 경향 |
| Qwen3-VL 8B | 8B ≈ 17.5GB | 압축(QLoRA) 필요 | 92.9% (4bit, 학습 전) **[실측]** | 추론 전용 재료로 사용 |
| **Qwen3.5-4B** | 4B ≈ 9.3GB | **압축 없이 학습 가능** (배치 1~2, 메모리 11.7~14.1GB) **[실측]** | **93.3%** (학습 전) **[실측]** | `baseline5` 환경(transformers 5.17.0) 전용. 생각 모드를 꺼야 함(`enable_thinking=False`). Apache-2.0 |
| Qwen2.5-VL-7B | 7B ≈ 16.6GB | 압축(QLoRA) 필요 | **93.5%** (4bit, 학습 전) **[실측]** | 추론 전용 재료로 사용 |
| InternVL3 / 3.5 (8B) | 8B | QLoRA/8bit | 글자·문서 판독에 강한 계열 **[가설]** | `internvl` 모듈은 있음. 사진 처리 방식이 다름 |
| MiniCPM-V 4.5 | 8B | 압축 필요 | 고해상도 글자에 강점 **[가설]** | 추가 설정(`trust_remote_code`) 필요 |
| Gemma 3 (4B/12B) | 4~12B | 4B 가능 | 글자 판독은 Qwen 대비 열세 **[가설]** | 앙상블 다양성용 |
| LLaVA / BLIP 계열 | - | - | 구세대, 글자 판독 약함 **[가설]** | 대회 소개에 언급되지만 **우선순위 낮음** |

**결정 (모델 대결 완료, §2.3)**
1. **학습 대상은 2개**: **Qwen3.5-4B(주력)** + **Qwen3-VL-4B(보조)**. 학습 전 점수 1등(Qwen2.5-VL-7B)을 고르지 않은 이유: ⓐ 신형 4개는 통계적으로 구분되지 않고 ⓑ 7B는 압축 없이 16GB 학습이 안 되며(압축은 정확도를 깎음) ⓒ 속도가 느림 (0.69초).
2. **7B·8B는 학습하지 않고 추론 전용 앙상블 재료**로 사용 (학습 없이도 93%대).
3. **채택 기준은 "같은 문제 기준 +2%p 이상 우세"** (§6.3). 신형끼리는 이 기준을 넘지 못했으므로 **속도·학습 용이성·오류 다양성**으로 골랐음.
4. 다양성을 위해 **다른 계열 모델**을 앙상블에 포함 (오답 겹침 Jaccard 0.31~0.37).
5. 학습 후 순위는 바뀔 수 있으므로 **학습 후 홀드아웃으로 다시 비교**.

### 4.2 사진 선명도(해상도) ⭐

> 💬 **쉬운 설명**
> 간판 사진을 **작게 줄여서** 보여주면 글자가 뭉개져서 AI도 못 읽습니다. 그래서 줄이면 손해가 확실합니다. 반대로 **원본 크기(약 880 토큰)까지 되돌리면 정확도가 크게 오르지만, 원본보다 더 키워도 사실상 오르지 않습니다.** (사진을 늘려도 원본에 없던 글자가 새로 생기지는 않기 때문입니다.) 결론: **원본 근처(768~880)로 맞추고 거기서 멈추면 됩니다.** 그 이상은 시간만 오래 걸립니다.

#### (1) "토큰"과 "실제 사진 크기"의 관계 — 헷갈리기 쉬운 부분

모델은 사진을 **28×28 픽셀 조각**으로 잘라서 보고, 조각 하나가 1토큰입니다. 즉 **토큰 수 = 사진 크기**입니다. 원본 사진(대부분 720×960)은 약 **880 토큰**이고, 아래는 각 설정이 사진을 얼마나 줄이거나 늘리는지입니다 (세로형 720×960 기준, 대략치).

| 설정(토큰) | 모델이 실제로 보는 크기 | 원본 대비 (가로·세로 각각) | 의미 |
|:---:|:---:|:---:|------|
| 188 (**베이스라인**) | 약 330×440 | 약 46% | **절반도 안 되게 축소** |
| 384 | 약 475×635 | 약 66% | 3분의 2로 축소 |
| 576 | 약 580×775 | 약 81% | 조금 축소 |
| **768** | **672×896** | **약 93%** | **거의 원본** (7%만 축소) |
| 약 880 | 728×952 | 100% | 원본 그대로 |
| 1024 | 약 775×1035 | 약 108% | 8% **확대** |
| 1536 | 약 950×1265 | 약 132% | 32% **확대** |

- ⚠️ 베이스라인의 `IMAGE_SIZE = 384`는 "384×384 정사각형"이 아니라 **총 픽셀 수를 384×384만큼으로 제한**하는 설정입니다. 그래서 세로 사진은 약 330×440으로 줄어듭니다.
- test 사진의 원본 크기는 하위 5% 약 750 토큰, 중앙값 약 880, 상위 5% 약 1,175 토큰입니다. 768로 지정하면 대부분은 살짝 줄고, 작은 사진 6%는 늘어납니다.
- 사진 파일을 **직접 리사이즈하지 마세요.** 모델 설정(`max_pixels`)으로 조절합니다. (사례: 손으로 크기를 맞추다 "image token count mismatch" 오류가 난 경우가 있음 **[선행사례]**. 크기는 **28의 배수**여야 함)

#### (2) 측정 결과 [실측]

Qwen2.5-VL-3B, 학습 없음, train 300장, 사진 크기를 강제 지정 (`min_pixels = max_pixels`, 확대 포함).

| 사진 크기(토큰) | 정확도 | 장당 시간 | test 6,714장 | GPU 메모리(추론) |
|:---:|:---:|:---:|:---:|:---:|
| 188 (베이스라인) | 80.0% | 0.15초 | 약 17분 | 7.7GB |
| 384 | 87.7% | 0.21초 | 약 24분 | 7.7GB |
| 576 | 88.3% | 0.32초 | 약 36분 | 7.8GB |
| **768** | **91.7%** | 0.44초 | 약 50분 | 7.9GB |
| 1024 | 91.7% | 0.62초 | 약 70분 | 8.0GB |
| 1536 | 93.0% | 1.04초 | 약 1시간 56분 | 8.2GB |

같은 300문제끼리의 짝 비교 (정확 이항검정):

| 비교 | 앞쪽만 정답 : 뒤쪽만 정답 | 판정 |
|------|:---:|------|
| 188 → 768 | 5 : 40 | ✅ **확실히 좋아짐** (p<0.001) |
| 384 → 768 | 4 : 16 | ✅ 좋아짐 (p=0.01) |
| 576 → 768 | 5 : 15 | ✅ 좋아짐 (p=0.04) |
| 768 → 1024 | 10 : 10 | ➖ **차이 없음** |
| 768 → 1536 | 9 : 13 | ➖ 우연일 수 있음 (p=0.52) |

**해석**
- **줄이면 손해는 확실**합니다 (글자가 안 읽힘). 손실은 **원본 크기(768 근처)에서 회복**됩니다.
- **원본보다 키우는 것은 손해도 없지만, 이득이 있다고 말할 근거도 부족**합니다. 1536이 숫자상 +1.3%p이나 4문제 차이(우연 범위).
- 다만 **표본이 300장(오차 ±3%p)** 이라 1~2%p의 작은 이득은 있어도 구분이 안 됩니다. 사진을 늘리면 모델이 작은 글자를 더 많은 조각으로 나눠 처리해 도움이 될 **가능성**은 남아 있습니다 **[가설]**.
- 파인튜닝 후에는 결과가 달라질 수 있습니다. 또 이 값은 3B 모델 기준이고 다른 모델(Qwen3-VL 등)은 최적점이 다를 수 있습니다.

#### (3) 권고 [실측 + 선행사례]

1. **기본 설정은 768~880 토큰.** 해상도를 더 올리는 데 시간을 쓰지 않습니다. (비용: 1536은 추론만 약 2시간, TTA 4번이면 그 4배)
2. **학습 때는 필요하면 중간 해상도(512~640)로** 메모리를 아끼고, **추론은 768~880**으로 하는 절충안도 시험할 만합니다 **[가설]** (메모리는 학습이 훨씬 빡빡함: 추론은 8GB 정도지만 학습은 활성화 메모리가 더 듦).
3. **고해상도(1024~1536)는 "헷갈리는 문제에만"** — 1등의 방식 **[선행사례]**: 기본은 256~1,024 토큰, **불확실한 문제 400개에만** 1,536 토큰으로 한 번 더 추론해 결과를 섞었습니다(`0.65 × 기존 + 0.35 × 고해상도`). 비용은 거의 안 늘고, 이득이 있다면 그 부분에서만 생깁니다. **Phase 3의 선택 항목**입니다.
4. 최종 후보에서 **홀드아웃 1,000장으로 1024 / 1536 이득이 확인될 때만** 전 문제 확대 적용.
5. ⚠️ **선행 사례 경고 [선행사례]**: 재활용품(색·재질) 사진에서는 전역 고해상도(896/1024)가 **이득이 없거나 오히려 하락**했고, 256~448px만으로도 상위권이 나왔습니다. 우리는 글자 사진이라 사정이 달라 **줄이면 손해**임이 실측됐지만, **키우는 쪽의 이득이 없다는 점은 두 대회가 일치**합니다.

#### (4) 그 밖에

- **하지 말 것**: 사진 **좌우 반전** — 글자가 거울처럼 뒤집혀 학습을 망칩니다. 색을 심하게 바꾸거나 크게 자르는 것도 (색·글자가 단서라) 피하세요.
- 선택: 작은 글씨(가격표)만 사진을 2~4조각으로 잘라 함께 넣는 방식. 오류 분석에서 필요성이 확인될 때만.

### 4.3 문제를 푸는(추론) 방식

> 💬 **쉬운 설명**
> ① AI가 "정답은 c입니다"라고 **말로 답하게** 하면, 가끔 이상한 말을 하거나 파악이 안 됩니다. ② 대신 **a·b·c·d 각각에 점수를 매기게** 하면 실패가 없고, 점수를 저장해 두었다가 나중에 **여러 결과를 합칠 수도** 있습니다. ③ 그리고 AI에게 **같은 문제를 보기 순서만 바꿔 4번** 풀리고 평균을 내면, "d를 덜 고르는 버릇"이 사라집니다.

| 방식 | 설명 | 평가 |
|------|------|------|
| 글로 답 생성 + 파싱 (베이스라인) | 생성한 문장에서 a~d 추출 | ❌ 파싱 실패 위험, 확률 없음 |
| **로짓 스코어링** (권장) | 정답 위치에서 a/b/c/d 4개 점수만 비교 | ✅ 실패 없음, 매번 같은 결과(재현성↑), **모든 상위권이 사용** |
| **보기 회전 TTA** | 보기 내용을 순환시켜 4번 풀고, 점수를 **원래 순서로 되돌려서** 평균 | ✅ 위치 버릇 상쇄. **1등의 핵심** (TTA4 → 0.951). 추론 시간 최대 4배 → 2가지로 절충 가능 |
| 위치별 점수 보정 | 모의고사에서 자리별 평균 확률을 재서 보정 | ✅ 비용 0, TTA와 병행 가능 |
| 보기 문장 자체의 확률 비교 | 각 보기 글자열이 나올 확률로 비교 | △ 별도 구현. 앙상블 다양성용으로 나중에 |
| CoT(길게 생각한 뒤 답) | 풀이를 쓴 뒤 답 | ❌ 지금은 비추천. 문제 대부분이 "읽기"라 이득이 작고 6,714장×긴 생성은 시간 부담 **[가설]** |

**⚠️ TTA 구현 함정 (실제 실패 사례 [선행사례])**
- 문제 여러 개를 묶어서(배치) 넣고 **오른쪽에 빈칸(패딩)을 채우면**, "마지막 위치의 점수"를 읽을 때 **빈칸을 읽게 되어** 예측이 완전히 망가집니다 (원본과 일치율 36%).
- **왼쪽 패딩**을 쓰거나 "진짜 마지막 글자 위치"를 명시적으로 읽으세요.
- 보기를 섞었으면 **점수를 원래 보기 자리로 되돌려서** 평균 내야 합니다.
- 만들고 나면 **"회전 전후 예측 일치율"** 을 꼭 확인하세요. 정확히 몇 %가 정상인지는 실험해 봐야 알지만(90%대 중후반일 것으로 **[가설]**), 선행 사례처럼 36%라면 버그입니다.

프롬프트는 **짧고 고정**을 권장합니다. 정답을 한 글자로만 출력하게 하는 구조라 문구를 바꿔도 이득이 작고, 학습하면 프롬프트 민감도는 더 줄어듭니다.

### 4.4 파인튜닝 (추가 학습)

> 💬 **쉬운 설명**
> 똑똑한 학생에게 **이 대회 기출문제 6,700개**로 과외를 시킵니다. 이때 세 가지가 중요합니다. ① **정답 칸만 채점**(문제 지문까지 채점하지 않기), ② **보기를 무작위로 섞어서 풀게 하기**(자리로 찍는 버릇 방지), ③ **너무 오래 하지 않기**(1~2바퀴면 충분, 오래 하면 외워버림).

**방법**: **LoRA (압축 없이 bf16)**. 전체 재학습은 16GB에서 불가능. **QLoRA는 4B 모델에 필요 없음** (아래 "QLoRA 정리").

> 💬 **QLoRA를 쓰면 정확도가 더 오를까? — 아니오 [실측]**
> LoRA는 모델에 작은 "메모장"만 덧붙여 학습하는 방법이고, QLoRA는 거기에 **모델 본체를 4bit로 압축**해 메모리를 아끼는 방법입니다. QLoRA는 **메모리가 모자랄 때 쓰는 절약 기법이지 정확도를 올리는 기법이 아닙니다.** 압축은 JPG처럼 본체를 조금 뭉개기 때문입니다.
> - **압축 자체가 정확도를 깎음**: 같은 모델을 압축 없이 vs 4bit로 추론 → Qwen3.5-4B **93.3% → 90.1% (−3.2%p)**, Qwen3-VL-4B **92.1% → 90.2% (−1.9%p)** (둘 다 통계적으로 뚜렷)
> - **4B는 압축 없이 16GB에서 학습됨**: bf16 배치 1~2에서 11.7~14.1GB (Qwen3.5), 10.9~15.6GB (Qwen3-VL). QLoRA는 오히려 **10~30% 느림**
> - QLoRA가 유용한 경우는 16GB에 안 들어가는 7~8B 이상 모델을 학습할 때뿐이며, 이 경우에도 이득은 "압축"이 아니라 "더 큰 모델을 쓸 수 있어서"임. 이번 비교에서는 8B·7B(4bit)가 4B(bf16)보다 낫다는 증거가 없었음.
> - ⚠️ 이전에 "Qwen3.5-4B는 압축 없이 학습한 선행 사례가 있다"고 적었던 것은 **오류**였음. 두 선행 사례 모두 4bit QLoRA였고, 우리는 직접 측정으로 bf16 가능을 확인함.
> - **미검증**: 학습으로 압축 손실이 회복되는지 (bf16 LoRA vs QLoRA를 같은 조건으로 학습해 비교해야 함, 각 약 2시간)

| 항목 | 권고 / 실제 설정 | 이유 |
|------|------|------|
| 학습 데이터 | train **전체 − 모의고사(1,000장)** = **5,714장** (`outputs/split/train_fit.csv`) | 베이스라인의 30배 |
| 채점 대상 | **정답 토큰 1개만** (나머지는 `-100`) | 상위권 공통. 신호를 집중 |
| 학습 대상 | **언어 모델 부분의 선형층**. 일반 어텐션(`q,k,v,o`)·MLP(`gate,up,down`) + **Qwen3.5는 선형 어텐션(`in_proj_qkv, in_proj_z, out_proj`)까지**. **사진 처리 부분은 동결**, 효과 부족하면 일부 해제 | ⚠️ **Qwen3.5-4B는 32개 층 중 24개가 선형 어텐션**이라 `q/k/v/o`만 잡으면 8개 층만 학습됨 (초기 메모리 시험은 이 점을 놓쳤음) **[실측]** |
| rank / 학습률 | r=16, α=32, dropout 0.05, lr 1e-4, cosine 스케줄(웜업 3%) | 상위권은 r16, lr 1e-4가 흔함 **[선행사례]**. 필요하면 r64 등으로 소규모 비교 |
| 학습 횟수 | 1 epoch | 정답이 1글자라 금방 외움. 상위권도 1 epoch **[선행사례]** |
| 유효 배치 | **8** (한 번에 1~2문제 × 모아서 4~8번) | 메모리 한계 때문. 선행 사례도 8 |
| 정밀도 | 4B: **압축 없이 bf16**, 7~8B: 학습하지 않음 (추론에만 4bit) | 위 QLoRA 정리 |
| 메모리 절약 | gradient checkpointing, 해상도 768 예산 | 16GB |
| 한 번에 올리는 문제 수 | Qwen3.5-4B: **1**, Qwen3-VL-4B: **2** (4까지 가능) | 배치를 키워도 빨라지지 않고 메모리만 넘침 |
| 예상 시간 | Qwen3.5-4B 1 epoch(5,714장) **약 2.4시간** (스텝당 0.2분 × 715), Qwen3-VL-4B 약 1.7~2시간 **[실측]** | 선행 사례: 4B QLoRA 63~79분 (사진이 작고 QLoRA·다른 설정) |

> ⚠️ **GPU 메모리 함정 (실제로 겪음) [실측]**: 학습 중 **실제 사용량은 최대 12GB**인데, PyTorch가 **예약해 두는 메모리가 문제 길이가 달라 조각나면서 17~19GB까지 불어났고**, 16GB를 넘으니 Windows가 시스템 메모리로 밀어내 학습이 **10배 이상 느려졌습니다** (10스텝이 10분 넘게 안 끝남). 해결: `PYTORCH_CUDA_ALLOC_CONF=garbage_collection_threshold:0.8,max_split_size_mb:512` + `torch.cuda.set_per_process_memory_fraction(0.92)`로 예약 상한을 두었더니 예약 13.3GB에서 안정, 스텝당 0.2분. (`src/train_lora.py`에 반영됨) 학습이 이상하게 느리면 `nvidia-smi`로 메모리가 16GB에 붙어 있는지 먼저 보세요.

**학습 때 데이터 변형(증강) — 허용됨 (규칙 §2)**
- ✅ **보기 순서 무작위 섞기 (정답 글자도 같이 이동)**: 위치 버릇을 근본에서 없애고 데이터를 사실상 4배로 활용. **상위권 공통, 가성비 최고**
- ✅ 아주 약한 밝기·대비 변화
- ⚠️ 회전·자르기는 글자가 잘리지 않는 범위에서만
- ❌ 좌우/상하 반전, 강한 색 변환

**2단계 학습 (선택) [선행사례]**
> 💬 첫 바퀴 후에 **아주 약한 세기(학습률 1/5)** 로 보기 섞기 학습을 **0.3바퀴만 더** 합니다. 처음에 크게 배우고, 나중에 살짝 다듬는 느낌입니다.
- 사례: 1단계 lr 1e-4 (1 epoch) → 2단계 lr 2e-5 + 보기 섞기 (0.3 epoch, 약 63분). **최종 Private에서 16문제 추가 정답**, Public에서는 5문제 개선이라 "Public만 보고 고르면 안 된다"는 근거가 됨.

**기대 효과**: 학습 전 기준선은 홀드아웃 **Qwen3.5-4B 93.3%, Qwen3-VL-4B 92.1%**입니다. 여기서 얼마나 오를지는 **학습 결과 대기 중 [가설]**. 신형 4개가 모두 틀린 문제가 1.9%이고 그중 상당수가 라벨 의심이라 **상한은 98% 안팎**일 수 있어, 개선 폭은 크지 않을 수 있습니다 (§2.3, §4.9).

**학습 실행**: `bash src/run_lora.sh` (Qwen3.5-4B → 홀드아웃 평가 → Qwen3-VL-4B → 홀드아웃 평가). 100스텝마다 홀드아웃 **앞 200장**으로 중간 확인(학습 중 이상 감지용일 뿐 선택 기준이 아님)하고 50스텝마다 저장하며, 끊기면 `--resume`으로 이어집니다. 어댑터는 `outputs/adapters/{tag}/final`, 평가는 `src/score.py --adapter`.

### 4.5 OCR(글자 인식 도구) 결합 — 선택

> 💬 **쉬운 설명**
> AI가 사진을 직접 읽는 대신, **전문 글자 인식 프로그램**이 먼저 글자를 뽑아 AI에게 힌트로 주는 방식입니다. 이론상 좋아 보이지만, 요즘 Qwen은 자체 글자 인식이 이미 강하고, **추가 프로그램 설치가 이 PC(최신 GPU)에서 말썽일 수 있습니다.**

| 방식 | 장점 | 단점 |
|------|------|------|
| AI 단독 (기본) | 단순, 재현성↑ | 작은 글씨·특이한 글꼴에서 오독 |
| OCR 텍스트를 프롬프트에 주입 | 글자열이 정확해질 수 있음 **[가설]** | 설치 부담, 입력 길이·시간 증가 |
| **보기 ↔ OCR 글자 유사도**를 추가 점수로 결합 | 보기가 짧은 글자라 구조적으로 적합 **[가설]** | 사진에 없는 답(추론형)엔 방해 |

**권고**: **우선순위 낮음.** 오류 분석에서 "AI가 글자를 잘못 읽어 틀린" 비율이 높게 나올 때만. 하더라도 프롬프트에 끼워 넣기보다 **추가 점수로 결합**하는 쪽이 안전. 최종 판단은 반드시 VQA 모델이 해야 함(규칙 §3).

### 4.6 앙상블(여러 AI 합치기)과 확률 저장

> 💬 **쉬운 설명**
> 서로 다른 AI 두 개가 **같은 문제를 다르게 틀린다면**, 두 점수를 평균 내면 정답에 가까워집니다. 이때 **"정답"만 저장하지 말고 a·b·c·d 각각의 점수(확률)를 파일로 저장**해 두세요. 나중에 다시 AI를 돌리지 않고도 **합치기 실험을 무한히 할 수 있습니다.** 우리 팀은 4~5명이니 **팀원끼리 점수 파일을 교환**하면 곧 앙상블 재료가 됩니다.

- **실측 [학습 전]**: 신형 4개의 확률을 **균등 평균하면 94.9%** (단일 최고 93.5%). 오답이 3분의 1만 겹침(Jaccard 0.31~0.37). 다만 **2개만 평균하면 92.4%까지 떨어지는 조합**도 있어 3개 이상 권장. 학습 후에는 모델들이 비슷해져 이득이 줄 수 있으니 **학습 후 재측정** ([MODEL_COMPARISON.MD](MODEL_COMPARISON.MD) §3).
- **소프트 평균이 정답**: 점수를 (가중) 평균. 선행 사례에서 **"복잡한 선택 규칙(라우터)보다 단순 평균이 압도적으로 좋았음"** **[선행사례]**.
- **강한 모델끼리만**: 약한 모델과 섞으면 오히려 손해 **[선행사례]**.
- **다양성의 원천**: 서로 다른 모델 계열 > 서로 다른 해상도. 선행 사례의 교훈: *"한 모델의 해상도를 여러 개 섞는 것보다 오류가 다른 다른 모델을 섞어라"* **[선행사례]**.
- 가중치는 **모의고사에서만** 정합니다 (test·리더보드로 튜닝 금지). 미세 조정(0.50→0.52 같은)은 **잡음 수준**일 수 있으니 조합 수를 적게.
- 일반적으로 강한 단일 모델 대비 **소폭(1~3%p) 개선 [가설]**. 시간 예산 안에서 설계 (예: 3B@768 = 49분/1회 → TTA×2 + 모델 2개 ≈ 3~4시간).

### 4.7 데이터 활용 확장

| 방법 | 설명 | 권고 |
|------|------|------|
| **dev 의사라벨** | (AI 예측 == 교육생 다수결)인 행만 학습에 추가 | △ 낮은 우선순위. 선행 사례에서 **오히려 점수 하락**, 1회차와 달리 우리 dev는 4명 이상 합의가 없음 |
| 외부 공개 데이터 | 규칙상 **공개된 데이터**면 허용. 후보: 한국어 OCR/장면 글자 데이터(AI Hub 등), TextVQA·ST-VQA 계열 | △ 라이선스·가용성 확인, **오프라인 대비 사전 다운로드**. 한국 거리 사진 도메인에 맞는 것만 |
| 틀린 문제 가중 | 1차 모델이 틀린 train 문제를 더 자주 학습 | ○ 오류 분석 후 저비용 |
| 보기 일부를 다른 문제 것으로 교체 | 오답 보기 합성 | ❌ 자연스러운 "비슷한 오답" 분포를 깨뜨릴 위험 |
| **외부 상용 AI(API)로 라벨 생성** | GPT/Gemini로 답 만들기 | ❌ **규칙 위반 소지** (§7) |
| 이미지 해시 일치 보정 | train과 사진·질문이 같으면 답 이동 | ❌ 우리 데이터에선 해당이 1건 뿐, 이득 극소 |

### 4.8 (참고) 선행 사례의 "불확실한 문제에만 힘 모으기" [선행사례]

1등이 마지막에 쓴 기법(+0.002 개선)입니다. **기본 결과에서 자신 없는 문제 1,000개만** 골라 보기 순서를 20가지 더 돌리고, 그중 400개는 더 선명하게 보게 하고, 500개는 **학습 전 원본 모델의 의견**과 엄격한 조건 하에서만 섞었습니다.

> 💬 이득이 작고 **개선 폭이 시험 잡음 수준(0.2%p)** 이라 우리는 **맨 마지막에 시간이 남을 때만** 검토합니다. 핵심은 "전부에 더 쓰지 말고 **헷갈리는 문제에만** 더 쓴다"는 발상입니다.

### 4.9 오류 분석 (나침반)

> 💬 **쉬운 설명**
> 시험 후 **틀린 문제 유형을 세어보는 것**입니다. "글자를 잘못 읽었나? 자리 버릇인가? 애초에 사진이 안 보이는 문제인가?"에 따라 처방이 달라집니다. 특히 **틀린 문제 50개를 직접 눈으로 보는 것**이 가장 값어치 있습니다.

| 오류 유형 | 판정 방법 | 처방 |
|-----------|-----------|------|
| 글자 오독 (비슷한 오답과 혼동) | 정답과 예측 보기의 글자 차이가 2자 이하 | 더 강한 모델, 조각 자르기, OCR 매칭, (원본 이하로 줄이고 있다면) 해상도 복원, 헷갈리는 문제에만 고해상도 |
| 위치 버릇 | 예측이 특정 자리에 쏠림 | TTA, 셔플 학습, 보정 |
| 부정형 오답 ("아닌 것은") | 질문에 부정어 포함 | 부정형 오버샘플링 |
| 수치·가격 오류 | 숫자 보기 | 조각 자르기, 헷갈리는 문제에만 고해상도 |
| 위치 관계 ("가장 오른쪽") | 위치어 포함 | 더 큰 모델, 해당 유형 오버샘플링 |
| **정답 오류 / 판독 불가 / 모호** | **사람이 직접 30~50장 확인** | 개선 불가 → **상한선 추정**에 사용 |

> 마지막 항목이 중요합니다. "사람도 못 맞히는 문제"의 비율을 알면 **어디서 멈출지** 정할 수 있습니다.

**실제 분석 결과 [실측, 학습 전]** — 상세: [ERROR_ANALYSIS.MD](ERROR_ANALYSIS.MD)

| 발견 | 내용 |
|------|------|
| 4개 신형 모두 틀린 문제 | **19개(1.9%)**. 사진으로 확인해 분류: **문항·라벨 문제 10건**(정답 여러 개 7 + 라벨 의심 3), **판독 곤란 6건**(작은 글씨, 가격표 중심), **진짜 모델 오류 3건** (잠정, 팀 검증 필요) |
| 진짜 모델 오류의 성격 | 글자를 그대로 읽지 않고 **익숙한 문구로 자동 보정**(`크라운 산도`→`샌도`, `미슐랭투스타`→`미슐랭 스타`), 숫자와 대상 짝짓기 실패 |
| **정확도 상한** | 약 **98~99%**. 현재 앙상블 94.9%에서 남은 여지 3.5~4%p의 대부분은 "다른 모델이 맞히는 문제"(Qwen3.5-4B 오답의 72%)를 잘 합치는 데서 나옴 |
| 라벨 노이즈 | 4개 중 3개 이상이 같은 오답: **홀드아웃의 3.0%**. train에도 비슷하게(약 170장) 있을 가능성 → 학습 후 점검 (§ERROR_ANALYSIS 3.3) |
| 유형 분류 오류 | "부정형" 52개 중 3개는 진짜 부정 질문이 아님(키워드 오분류). 진짜 부정 질문 정확도 90.9% |

### 4.10 모델·라이브러리 버전 문제 (새로 확인된 실무 이슈)

> 💬 **쉬운 설명**
> AI 프로그램(transformers)에는 버전이 있는데, **가장 최신 모델(Qwen3.5)은 새 버전(5.x)에서만 돌아갑니다.** 그런데 대회용으로 미리 받아둔 환경은 옛 버전(4.57.6)에 묶여 있어서 **그대로는 안 됩니다.** 그렇다고 기존 환경을 지우면 안 됩니다 (가이드가 "가상환경을 삭제하지 마세요"라고 명시). 그래서 **환경을 하나 더 만들어서** 따로 시험해야 합니다.

- 기존 환경 `baseline`: `transformers 4.57.6` (`<5.0.0` 고정), `torch 2.11.0+cu128`, `peft 0.21.0`, `bitsandbytes 0.50.2` **[확인]**. **`qwen2_5_vl`, `qwen3_vl`, `qwen3_vl_moe`, `internvl`, `gemma3` 사용 가능**, **`qwen3_5`는 없음**.
- ✅ **해결**: 두 번째 가상환경 **`baseline5`** 를 만들었습니다 (`transformers 5.17.0` 정식판, `peft 0.21.0`, `accelerate 1.15.0`, `bitsandbytes 0.50.2`, torch는 기존 wheel 재사용). **기존 `baseline`은 그대로 유지**하여 지금까지의 실험이 계속 재현됩니다.
- ✅ **오프라인 대비**: wheel 80개(142MB)를 `가이드 및 코드/downloads/libs5/`에 보관, 패키지 목록은 `outputs/baseline5_requirements.txt`. **인터넷 없이 새 환경에 설치되는 것을 임시 환경에서 검증**함 (Windows 경로 길이 제한 때문에 짧은 경로에서 시험해야 함).
- ✅ **모델 4개 사전 다운로드 완료** (Qwen3.5-4B, Qwen3-VL-4B/8B, Qwen2.5-VL-7B, 합계 약 52GB, 전부 Apache-2.0).
- **사용법**: Qwen3.5 → `baseline5`, 그 외 → `baseline`. (`src/README.MD` 참고)
- ⚠️ **Qwen3.5는 생각 모드를 꺼야 함**: 기본 설정이면 답 자리에 `<think>`가 붙어 a/b/c/d 점수를 읽는 위치가 틀어집니다. `apply_chat_template(..., enable_thinking=False)` 필수 (다른 모델에서는 무시되어 결과 동일함을 확인).
- Qwen3.5는 전용 가속 커널이 없어 느린 대체 경로로 동작하지만(경고 메시지), 그래도 4개 중 가장 빠릅니다.

---

## 5. 권장 로드맵

> 원문 기준 종료까지 7일 남음(복사 시점). 실제 마감은 대회 페이지에서 확인 후 조정하세요.
> 각 단계의 **통과 기준(Gate)** 을 못 넘으면 다음으로 가지 말고 **원인부터** 파악합니다.

> 💬 **쉬운 설명**
> ① 먼저 **시험지(모의고사)와 채점기**를 만들고 ② **학습 없이** 여러 AI와 설정을 비교해 후보를 좁히고 ③ 좁힌 후보를 **6,700문제로 추가 학습**시키고 ④ 결과들을 **평균**낸 다음 ⑤ 마지막에 **깨끗한 환경에서 그대로 재현되는지** 확인해서 제출합니다.

### Phase 0 — 기반 구축 (0.5~1일)

- [x] 이미지 해시 기준 **모의고사 1,000장** 분리 (질문 유형 고르게) → 이후 **절대 학습에 쓰지 않음** (`src/make_holdout.py`, `outputs/split/`)
- [x] **채점기** 작성: 로짓 스코어링, 정확도·신뢰구간·유형별 정확도, **점수(a~d 확률) 파일 저장** (`src/score.py`)
- [x] 3B 학습 없이 @768 토큰으로 모의고사 **기준선 88.5%** 확정, test 제출 파일 생성. [x] **리더보드 제출 결과 Public 0.88650 → 홀드아웃 88.5%와 +0.15%p 차이로 거의 일치** (기록: `outputs/leaderboard_log.csv`)
- [x] 재현성 설정 (seed 고정, 라이브러리 버전 고정, 무작위 생성 끄기) — ⚠️ **같은 조건 재실행 시 정확히 같은 결과가 나오는지는 아직 검증 안 함**
- **Gate**: 같은 조건으로 다시 돌렸을 때 **모의고사 정확도가 정확히 같은가** (미검증)

### Phase 1 — 모델·해상도·추론 탐색 (1일, 학습 없음)

- [x] 해상도: 3B로 188~1536 토큰 6단계 측정 완료 (300장, §4.2). 홀드아웃 1,000장 재확인은 768 토큰 한 가지만 함 [ ] (880·1024 재확인은 선택)
- [ ] 위치 버릇 확인 + **보기 회전 TTA(2가지·4가지)와 위치 보정**의 효과 측정 (**left padding 함정 주의, 회전 전후 일치율 확인**) — 위치 버릇 자체는 확인함(§2.3), **TTA는 미구현**
- [x] **모델 대회**(홀드아웃 1,000장, 같은 조건): Qwen3.5-4B, Qwen3-VL-4B/8B, Qwen2.5-VL-7B(4bit) → **학습 대상 2개 선정** (Qwen3.5-4B, Qwen3-VL-4B). 결과: [MODEL_COMPARISON.MD](MODEL_COMPARISON.MD). InternVL3.5-8B는 시험하지 않음
- [x] 틀린 문제 분석 (§4.9): 4개 전부 틀린 19문제를 **전수 육안 확인·분류 완료** ([ERROR_ANALYSIS.MD](ERROR_ANALYSIS.MD), 판정표 `outputs/probs/error_review_19.csv`). [ ] **판정은 제 주관이므로 팀원의 검증 필요** (특히 그리스 문자 비석, KUSHIR, Blue Label)
- **Gate**: 후보가 **같은 문제 기준 +2%p 이상** 우세할 때만 채택 → **신형끼리는 미달**이라 속도·학습 용이성·오류 다양성으로 선택 (§4.1)

### Phase 2 — LoRA 추가 학습 (2~3일)

- [x] 시범 실행으로 **속도·메모리 측정** → 전체 학습 시간 확정 (§4.4, 메모리 함정 발견·해결)
- [ ] 🔄 전체 train 학습 (**정답 토큰만 채점, 보기 섞기, 유효 배치 8, 1 epoch**, 압축 없이 bf16): Qwen3.5-4B 진행 중 → Qwen3-VL-4B 대기
- [ ] 소규모 비교 (**한 번에 한 가지씩만 바꾸기**): 학습 해상도, 사진 처리부 학습 여부, r 16 vs 64
- [ ] (선택) **2단계 학습**: lr 1/5, 0.3 epoch, 보기 섞기 (§4.4)
- [ ] 바퀴(epoch)마다 모의고사 정확도 기록, 체크포인트 보관
- **Gate**: 학습 없음 대비 개선이 **통계적으로 의미 있는가**(§6). 개선이 없으면 설정 탓하기 전에 **데이터·오류 분석**으로 돌아가기

### Phase 3 — 결합·후처리 (1일)

- [ ] 모델별 점수 파일 → **소프트 평균**(가중치는 모의고사에서만 결정). 팀원 결과도 합치기
- [ ] TTA 최종 확정 (시간 예산 내)
- [ ] (선택) **헷갈리는 문제에만 고해상도(1024~1536) 추가 추론**(§4.2-3, §4.8), OCR 결합 — **오류 분석이 요구할 때만**
- **Gate**: 앙상블이 최고 단일 모델을 모의고사에서 이기는가

### Phase 4 — 마무리 (0.5~1일, 여유 포함)

- [ ] **깨끗한 환경에서 처음부터 다시 실행해 같은 CSV** 가 나오는지 확인
- [ ] 제출물 3종 정리: `submission.csv`, 학습·추론 코드, 데이터 처리·모델 개발 설명 문서 (실험 기록표가 문서 초안)
- [ ] 최종 제출 선택 (직접 선택하지 않으면 자동 선택됨)
- [ ] **제출 예산**: 하루 20회 중 **매일 5회 이상은 최종 튜닝용으로 남기기** [선행사례: 20회를 다 써서 유망 후보를 못 낸 사례]

### 우선순위 표 (선행 사례 반영해 조정)

| 작업 | 기대 이득 | 비용 | 위험 | 순서 |
|------|:---:|:---:|:---:|:---:|
| 사진을 원본 크기(768~880)로 맞추기 | 높음 **[실측]** | 매우 낮음 | 낮음 | 1 |
| 원본보다 더 키우기 (1024~1536, 전체) | 불확실 (768→1024 차이 없음, 768→1536 우연 범위) **[실측]** | 높음 (추론 1.4~2.4배) | 낮음 | 5 (헷갈리는 문제에만 선택 적용) |
| 로짓 스코어링 + 위치 보정 | 중 | 매우 낮음 | 낮음 | 1 |
| **학습 때 보기 섞기** | 중 **[선행사례]** | 매우 낮음 | 낮음 | 2 |
| **보기 회전 TTA (올바른 구현)** | 중~높음 **[선행사례]** | 중 (추론 2~4배) | 낮음 (구현 함정) | 2 |
| 전체 train LoRA (정답 토큰 채점) | 중~높음 | 중~높음 | 중 | 3 |
| 모델 세대 교체 (Qwen3-VL 등) | 중~높음 **[선행사례]** | 중 | 중 (메모리·호환) | 2(탐색), 3(학습) |
| 2단계 저 lr 학습 | 낮음~중 **[선행사례]** | 낮음(≈1시간) | 낮음 | 4 |
| 소프트 앙상블 (강한 모델끼리) | 낮음~중 | 높음(시간) | 낮음 | 4 |
| OCR 결합 | 불확실 | 높음 | 높음 (설치·호환) | 5 |
| dev 의사라벨 | 낮음/음수 가능 **[선행사례]** | 중 | 중 (노이즈) | 5 |
| 라우터·복잡한 선택 규칙 | 음수 가능 **[선행사례]** | 중 | 높음 (과적합) | 제외 |
| CoT / Thinking | 낮음 | 매우 높음 | 중 | 제외 |

---

## 6. 검증 설계

> 💬 **쉬운 설명**
> **연습 시험(내가 만든 모의고사)** 점수로 결정하고, 대회 리더보드는 "참고용 체온계"로만 쓰세요. 리더보드 점수에 맞춰 계속 고치면 **기출 외우기**가 되어 최종 시험에서 점수가 떨어집니다. 실제로 선행 사례 2명이 이 함정에 빠졌습니다.

### 6.1 원칙

> ✅ **검증 완료 [실측]**: 첫 제출(3B, 학습 없음)에서 **홀드아웃 88.5% ↔ Public 0.88650**으로 **차이 +0.15%p**였습니다. Public은 test 일부(약 3천 문제 규모)라 자체 오차가 ±1%p 안팎이고 홀드아웃 오차는 ±1.9%p이므로, 이 정도 일치면 **홀드아웃이 리더보드를 잘 대변하는 시험**이라고 볼 수 있습니다. 다만 **한 번의 비교**이고 학습 후 모델에서도 그런지는 계속 확인해야 하며, **1~2%p 이하의 차이는 여전히 잡음**입니다.

1. **홀드아웃(train에서 뺀 약 1,000장)이 유일한 의사결정 기준.** 리더보드는 확인용.
2. 분할은 **이미지 해시 기준**(같은 사진이 양쪽에 들어가지 않게) + 질문 유형 고르게.
3. **dev는 검증에 쓰지 않음** (§1.3).
4. 리더보드는 하루 **20회 제한**. 리더보드 점수에 맞춰 튜닝하면 Public → Private에서 순위가 흔들릴 수 있음 (Kaggle 규칙 §7: 최종 순위는 Private).
5. **탐색 조합 수를 제한**: 수천 가지를 시험하면 그중 하나는 "우연히" 좋아 보입니다 (선행 사례: 2,448가지 탐색이 오히려 하락).

### 6.2 표본 크기와 신뢰구간

> 💬 표본이 적으면 점수가 **운에 크게 좌우**됩니다. 150문제로 잰 90%는 "85~95% 어딘가"라는 뜻입니다.

| 모의고사 크기 | 정확도 90% 부근 95% 신뢰구간 | 해석 |
|:---:|:---:|------|
| 150 (이번 실측) | ±4.8%p | 방향성만 |
| 256 (선행 사례) | ±3.7%p | 작음 |
| 500 | ±2.6%p | 큰 차이 판별용 (모델 대회) |
| 1,000 | ±1.9%p | 기본 모의고사 |

- 1%p 단위의 개선은 독립적으로는 구분 불가. **같은 문제에 대한 짝 비교(McNemar 검정)** 로 판단. 그래도 애매하면 "이득 없음"으로 보고 **단순한 쪽** 선택.

### 6.3 채택 규칙 (미리 합의)

- 새 기법은 모의고사 **짝 비교에서 개선이 재현**될 때만 채택 (예: 2개 seed에서 모두 개선).
- **개선의 정도와 무관하게 "복잡도 대비 이득"** 이 명확할 때만.
- 미달이면 복잡도가 낮은 쪽 유지.

### 6.4 실험 기록

- 실험 ID · 모델 · 해상도 · LoRA 설정 · seed · 모의고사 정확도(+유형별) · 추론 시간을 표 한 장에 누적
- 모델별 모의고사·테스트 **확률 파일 저장** → 앙상블·재현성·사후 분석의 기반
- **위험한 실험 전에는 현재 최고 결과물을 반드시 보존** [선행사례]

---

## 7. 규정·제약 체크

| 항목 | 규정 | 이 전략에서의 대응 |
|------|------|-------------------|
| API 추론 | **API 호출/응답을 이용한 추론 엄격히 금지** | 모든 추론은 로컬 모델. **상용 API로 라벨을 만드는 것도 위험하므로 사용하지 않음** |
| 사전학습 모델 | Hugging Face 모델 허용, **VQA 모델 활용** | Qwen-VL 계열은 대회 제공 베이스라인과 같은 계열. 다른 VLM이 "VQA 모델"에 해당하는지 의문이면 **운영진에 문의** |
| 외부 데이터 | 허용 (**공개적으로 사용 가능한 데이터**만) | 출처·라이선스를 문서에 기록 |
| 데이터 증강 | 허용 | 보기 섞기, 약한 밝기 변화 등 |
| Data Leakage | 테스트 데이터 사전 접근·유출 금지 | test로 학습·튜닝·가중치 결정 금지. 테스트 이미지를 사람이 라벨링하는 것도 Kaggle §4b에서 금지 |
| 재현성 | 코드 실행 시 동일한 결과, 종료 후 주최 측 검증 | seed·버전 고정, 무작위 생성 끄기, 확률 저장, 깨끗한 환경 재실행 |
| 제출 횟수 | 팀당 하루 최대 20회 | 모의고사에서 고른 후보만 제출, 매일 5회 이상 여유 |
| 제출물 | CSV + 모델 코드 + 추가 문서 | 실험 기록표(§6.4)가 문서 초안 |
| 팀 간 공유 | 팀 외부에 **비공개 공유 금지**, 공개는 포럼/노트북으로만 (Kaggle §5d, §6) | **팀 내부**의 점수 파일·코드 공유는 자유. 다른 팀과 개인 채널 공유는 금지 |
| 오픈소스 라이선스 | Kaggle §6c: OSI 승인 + 상업 이용 제한 없는 라이선스 (대회별 규칙에서 달리 정하지 않는 한) | **제공된 Qwen2.5-VL-3B는 `Qwen RESEARCH LICENSE`** (모델 폴더의 LICENSE로 확인). 대회가 직접 제공했으므로 문제 소지는 낮으나 **Apache-2.0 계열 권장**, 사용 모델의 라이선스는 문서에 명시 **[가설: 모델별 라이선스는 각 model card에서 확인 필요]** |
| 오프라인 환경 | 오프라인 베이스라인은 대회 중 다운로드하지 않음 | **사용할 모든 모델·라이브러리를 사전 다운로드** (§9) |

---

## 8. 리스크와 대응

| 리스크 | 영향 | 대응 |
|--------|------|------|
| 대회 당일 인터넷 차단 / 다운로드 부하 | 후보 모델·라이브러리 사용 불가 | 후보 전부 **미리 다운로드**, wheel 보관 |
| **Qwen3.5용 `transformers 5.x` 미설치** | Qwen3.5 사용 불가 | 별도 가상환경에서 사전 검증 (§4.10). 못 하면 Qwen3-VL로 진행 |
| 16GB 메모리 한계 (7~8B, 고해상도 학습) | 메모리 부족, 학습 시간 폭증 | 시범으로 사전 측정, 해상도·배치 절충, 3~4B 우선 (4B 두 모델은 bf16 학습 가능 확인) |
| **GPU 예약 메모리가 16GB 초과 (실제 발생)** | **Windows가 시스템 메모리로 밀어내 10배 이상 느려짐** (실사용은 12GB인데 예약이 17~19GB로 불어남) | 예약 상한(`set_per_process_memory_fraction(0.92)`)+`garbage_collection_threshold`, 학습이 느리면 `nvidia-smi`부터 확인 (§4.4) |
| 압축(4bit)으로 정확도 하락 | 4B에서 −1.9~−3.2%p | 4B는 압축 없이 학습·추론. 7~8B의 4bit 결과는 실제보다 낮게 나온 값일 수 있음 |
| 최신 GPU(5060 Ti) 호환성 | 일부 라이브러리(flash-attn 등) 설치 불가 (현재 환경에는 없음 **[확인]**) | 기본 SDPA attention, 추가 의존성 최소화 |
| **TTA 구현 오류 (패딩 위치)** | 예측 붕괴 (일치율 36% 사례) | left padding, 회전 전후 일치율 점검 (§4.3) |
| 모의고사 과적합 | 최종 성능 과대 추정 | 후보 수 제한, 짝 비교, 채택 규칙 |
| Public↔Private 격차 | 순위 하락 | 리더보드 튜닝 금지, 모의고사 기준 |
| 학습 정답의 노이즈 | 학습 상한 저하 | 오류 분석에서 육안 확인 |
| 재현 불가(연산이 매번 달라짐) | 검증 실패 | 결정적 추론·seed·버전 고정, 깨끗한 환경 재실행(Phase 4) |
| 제출 횟수 소진 | 최종 후보를 못 냄 | 매일 5회 이상 예약 |
| 시간 부족 | 완성도 저하 | Phase 순서 엄수 (0→1이 이미 큰 이득). 마감 24시간 전 동결 |

---

## 9. 사전 준비 체크리스트 (오프라인 대비)

- [x] 사용할 모델 다운로드 완료: Qwen3.5-4B, Qwen3-VL-4B/8B, Qwen2.5-VL-7B (`src/download_models.py`, 합계 약 52GB + 기존 3B 7.1GB)
- [x] 기존 환경 wheel(`downloads/libs`, `transformers<5.0.0`)은 그대로 유지
- [x] **두 번째 가상환경 `baseline5`** (transformers 5.17.0) + wheel 보관(`downloads/libs5`) + **인터넷 없이 설치되는 것 검증**
- [x] 오프라인 환경변수(`HF_HUB_OFFLINE=1`, `TRANSFORMERS_OFFLINE=1`) 상태에서 모델 로드 (모든 스크립트가 기본으로 설정, 4개 모델 모두 로드·추론 확인)
- [x] 디스크 여유 확인 (C: 약 768GB 여유 → 모델 추가 후에도 충분)
- [ ] (선택) 외부 데이터·OCR 라이브러리를 쓸 경우 wheel/데이터도 함께 준비
- [ ] **대회 당일 오프라인 환경에서 `baseline5`로 전체 파이프라인이 실제로 도는지 최종 리허설** (가상환경을 다른 PC로 옮기면 경로가 깨질 수 있으니 wheel 재설치 방식 권장)

---

## 10. 지금 당장 할 일 (초보자용 요약)

**완료된 것** ✅
1. 모의고사 1,000장 분리 · 2. 채점기 · 3. 기준선(3B 88.5%) · 5. 모델 다운로드와 5개 모델 비교 (신형 4종 92.1~93.5%, 평균 94.9%)

**지금 진행 중** 🔄
- 전체 train 추가 학습 (정답 칸만 채점 + 보기 섞기, **압축 없이 bf16**): Qwen3.5-4B(약 2.4시간) → Qwen3-VL-4B(약 2시간)

**다음 할 일**
1. 학습이 끝나면 각 모델의 홀드아웃 정확도를 학습 전과 **짝 비교**로 확인 (`outputs/experiments.csv`의 `q35_4b_lora_t768`, `q3vl_4b_lora_t768`)
2. **선지 회전 TTA 구현**(왼쪽 패딩 주의, 회전 전후 일치율 확인) → d 편향·정확도 개선 측정
3. **사람이 할 일**: 4개 모델이 모두 틀린 19문제를 눈으로 확인 (라벨 오류인지) — `outputs/probs/holdout_all_new_models_wrong.csv`
4. ✅ ~~리더보드 제출로 홀드아웃과 비교~~ **완료**: 홀드아웃 88.5% ↔ Public 0.88650. 이후 제출마다 `outputs/leaderboard_log.csv`에 기록해 두 점수의 차이를 계속 추적
5. 학습된 모델들의 **test 확률 파일**을 만들어 앙상블 (학습 전 4개 평균이 94.9%였으므로 기준으로 삼기)

> 모르는 부분이 있으면 [용어 사전](#-용어-사전-먼저-읽어-보세요)과 각 절의 💬 박스를 먼저 보시고, 그래도 어려우면 어느 절인지 알려 주세요.

---

## 11. 부록 — 측정 프로토콜 (재현용)

- 모델: `downloads/models/Qwen2.5-VL-3B-Instruct` (bf16, `device_map="cuda"`, SDPA attention)
- 데이터: `train.csv`에서 `sample(n=150, random_state=0)` / dev는 다수결 3명 일치 행에서 150개
- 프롬프트: 베이스라인의 `SYSTEM_INSTRUCT` + `build_mc_prompt` 그대로
- 채점: 생성 시작 위치에서 `a,b,c,d` 4개 토큰 점수(로짓)의 최댓값 (`model(**inputs).logits[0,-1,ids]`)
- 해상도: `min/max_pixels = 384²`(베이스라인) 대 `max_pixels = 768×28×28`, `min_pixels = 256×28×28`
- 하드웨어: RTX 5060 Ti 16GB, 배치 1
- 측정 결과: train(정답) 82.0% → 90.0% / dev(교육생 다수결 일치) 52.7% → 57.3% (dev는 **정답이 아니므로 참고용**)

**해상도 6단계 측정 (§4.2)**
- 표본: train에서 `sample(n=300, random_state=1)` (위 150장 표본과 다름)
- 사진 크기: `min_pixels = max_pixels = 토큰 수 × 28 × 28` (**확대 포함 강제 지정**, 비율 유지, 28의 배수로 반올림). 토큰 수 = 188 / 384 / 576 / 768 / 1024 / 1536
- 결과: 80.0 / 87.7 / 88.3 / 91.7 / 91.7 / 93.0 (%), 장당 0.15 / 0.21 / 0.32 / 0.44 / 0.62 / 1.04초, 추론 GPU 메모리 7.7~8.2GB
- 짝 비교: 정확 이항검정(같은 문제에서 한쪽만 맞힌 수 비교)
- 한계: 표본 300장(±3%p), 학습 전 3B 모델 한 종류

> 측정 스크립트를 프로젝트 폴더에 저장해 두면 Phase 0의 채점기 출발점으로 바로 쓸 수 있습니다.
